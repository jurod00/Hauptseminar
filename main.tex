% Link to this project: https://www.overleaf.com/read/hdpkgxgjkbgw

%% TeXstudio and TeXShop accept magic comments
% !TeX encoding = utf8
% !TeX spellcheck = en_GB
%% !BIB program = biber

\RequirePackage[l2tabu, orthodox]{nag} % at beginning to track errors
\documentclass[a4paper, english, 11pt, onecolumn, oneside, final]{article}

\usepackage[T1]{fontenc}        % Use 8-bit encoding that has 256 glyphs, for output
\usepackage[utf8]{inputenc}     % to input ä, ß, ø, æ, å, „“, “” etc. (incl. bib)
\usepackage[verbose, a4paper, hcentering]{geometry}	% customize page layout, total={418.3pt, 591.5pt}, height=.9\paperheight

\geometry{a4paper}
%\usepackage{color}	            % enables colors
\usepackage[english]{babel}     % English (as References, e.g.)
\usepackage{amsthm, mathtools, amssymb, nicefrac} % mathtools loads amsmath
\usepackage{algorithm2e}        % enables algorithms
%\mathtoolsset{showonlyrefs}
\numberwithin{equation}{section}   % numbering equations

%   ╭════════════════════════════════════
%	│	Developers tools
\usepackage[notcite,notref, draft]{showkeys}	%  display labels and bookmarks
\setlength {\marginparwidth }{2cm}  % just avoid a package warning
\usepackage[textsize= tiny, obeyFinal]{todonotes}  %  enable \todo{...}


%	╭───────────────────────────────────────────────
%	│	User preferential fonts
%\PassOptionsToPackage{full}{textcomp}
\usepackage{newtxtext, newtxmath}%	replaces Springer like postscript (PS) fonts (Times)
%\usepackage{lmodern}           % improved pdf fonts
\usepackage{dsfont}             % für \one und Erwartungswert, doublestroke
\usepackage{eurosym}
\usepackage{microtype}          % after fonts, for typographical perfection

%	╭───────────────────────────────────────────────
%	│	tables and figures
\usepackage{wrapfig}    % enables figures floating in the text
\usepackage{caption, subcaption} % replaces deprecated subfig
%\usepackage{graphicx}  % tikz loads this package as well
%\setkeys{Gin}{draft}   % draft= false
\usepackage{array}      % extends array and tabular environments
\usepackage{booktabs}   % more beautiful tables
\usepackage{tikz}       % nice tool for professional figures
\usepackage{svg}

%	╭───────────────────────────────────────────────
%	│	references and enumerations
\usepackage{siunitx}    % detect-all; [locale=DE] for German commas, etc
\DeclareSIUnit{\sieuro}{\mbox{\euro}}   % provide € symbol in formulas


\usepackage[inline, shortlabels]{enumitem}           % customized list environments, as roman
\setlist[enumerate,1]{label=(\roman*)}  % 1st layer (i)
\usepackage[numbers]{natbib}	% References: round, numbers
\usepackage[bookmarks=true, 
    pdfpagemode= UseNone, 
    backref= page, % include back-reference in references
    pdftitle={Scientific writing}, 
    pdfauthor={Alois Pichler}, 
    colorlinks=true, citecolor=ForestGreen, urlcolor=darkgray, linkcolor=ForestGreen, 
    pdfstartview= FitH]{hyperref} % load as last package
\usepackage{doi}                % provides proper doi-links in references: load after hyperref
\definecolor{ForestGreen}{HTML}{228B22} %005f50

\usepackage[capitalise]{cleveref}   % lazy referencing
\usepackage[all,defaultlines=2]{nowidow} % Hurenkinder, Schusterjungen

%	╭───────────────────────────────────────────────
%	│	User specified commands
\definecolor{TUC}{RGB}{0, 95, 80}%\normalcolor
\definecolor{Alois}{RGB}{161, 11, 112}
%%   response letter
\newenvironment{response}[1][blue]{\par\color{#1}\vspace{0.4em}\itshape\textbf{Response: }}{\par}			% response
%%   revision
\newcommand{\rev}[2][blue]{{\color{#1}#2}}       % command
\newenvironment{revise}[1][blue]{\color{#1}}{}   % environment

%%   my commands

\DeclareMathOperator{\var}{\mathrm{var}}    % variance
\DeclareMathOperator{\cov}{\mathrm{cov}}	% covariance
\DeclareMathOperator{\pr}{\mathrm{Pr}}     % Probability
\DeclareMathOperator{\E}{\mathrm{E}}	    % Expectation
\DeclareMathOperator*{\essinf}{ess\,inf}    % essential supremum
\DeclareMathOperator*{\esssup}{ess\,sup}    %
\DeclareMathOperator*{\argmax}{arg\,max}
\DeclareMathOperator{\VaR}{\mathrm{VaR}}	% Value-at-Risk
\DeclareMathOperator{\AVaR}{\mathrm{AVaR}}	% Average Value-at-Risk

\newcommand{\R}{\mathbb{R}}                 % real numbers
\newcommand{\one}{\mathds 1}
\newcommand{\precision}{\Sigma^{-1}}
\newcommand{\A}{r^\top\Sigma^{-1} r}
\newcommand{\B}{r^\top\Sigma^{-1}\one}
\newcommand{\C}{\one^\top\Sigma^{-1}\one}
\newcommand{\D}{(\A)(\C)-(\B)^2}

%%%%%%%%%%%%%%%%%%%%%%%%% the "\LyX" command.
\providecommand{\LyX}{\texorpdfstring{L\kern-.1667em\lower.25em\hbox{Y}\kern-.125emX\@}{LyX}}


%	╭───────────────────────────────────────────────
%	│	math environments for Theorems, Definitions, Remarks etc.
\theoremstyle{plain}
	\newtheorem{theorem}             {Theorem}[section]
	\newtheorem{corollary}  [theorem]{Corollary}
	\newtheorem{lemma}      [theorem]{Lemma}
	\newtheorem{proposition}[theorem]{Proposition}
\theoremstyle{definition}
	\newtheorem{definition} [theorem]{Definition}
\theoremstyle{remark}
	\newtheorem{remark}     [theorem]{Remark}
    \newtheorem{claim}      [theorem]{Claim}

%	╭───────────────────────────────────────────────
%   │     the main document
\title{
    Sensitivity of Optimal Solutions to the Markowitz Portfolio Optimization Problem under certain Risk Measures
}

\author{
    Julius Rode%
        \thanks{Chemnitz, University of Technology. Contact: \href{mailto:julius.rode@s2018.tu-chemnitz.de}{julius.rode@s2018.tu-chemnitz.de}}
}

%  █▀▙ █▀█ █▀▀ █░█ ▙▃▟ █▀▀ █▖█ ▀█▀
%  █▄▛ █▄█ █▄▄ █▄█ █▔█ ██▄ █▝█ ░█░
\begin{document}
\maketitle

\begin{abstract}
"Don't put all your eggs in one basket." is a fundamental investment strategy. But how many eggs should we specifically put in our different baskets? The Markowitz\footnote{Harry Max Markowitz (1927-2023) was an American economist and Nobel Memorial Prize laureate in Economic Sciences in 1990}model, invented in 1952, provides a mathematical approach to this question. In addition, we introduce two more Markowitz-like models equipped with different risk measures. So that an investor may choose a model satisfying his risk aversion or risk preference. Our goal is to elaborate on their solutions, give a numerical approach, and determine the sensitivity of the respective model. Like with highly sensitive weather models, small changes in temperature, air pressure, etc. cause different weather outcomes and a rainy vacation. Which means a model's sensitivity measures how the solution of the model reacts to small changes in the input parameters. We apply this concept of sensitivity to the following three finance models and investigate their behaviors under small changes, e.g. minimum return, stock prices, or risk aversion. The project can be retraced on GitHub and is available here: \href{https://github.com/jurod00/Markowitz}{https://github.com/jurod00/Markowitz}.\medskip

\noindent\textbf{Keywords:} Markowitz · Risk Measures · Optimization\par
\noindent\textbf{Classification:} 91G10 · 91G30 · 91G70
\end{abstract}
\section{Introduction}
For the sake of convenience, a real-valued random variable always maps to the measurable space $\R=(\R,\mathcal{B}(\R))$ with abuse of notation, while $\mathcal{B}(\R)$ is the Borel\footnote{F\'elix \'Edouard Justin \'Emile Borel (1871-1956) was a French mathematician}$\sigma$-algebra on $\R$. \medskip

This elaboration analyzes Markowitz-like models with different risk measures, i.e. concretely we are looking for an allocation vector $x\in\R^J$ that optimizes the following problems
\begin{equation}
    \label{problem:Markowitz}
    \begin{aligned}
        & \underset{x\in\R^J}{\mathrm{minimize}} & & \var x^\top\xi \\
        & \mathrm{subject\,to} & & \E x^\top\xi \geq \mu\;\;\;\;\;\;\;\;\;\;\;\; \\
        & & & x^\top\one = 1
    \end{aligned}
\end{equation}
\begin{equation}
    \label{problem:UtilityMaximization}
    \begin{aligned}
        & \underset{x\in\R^J}{\mathrm{maximize}} & & \E x^\top\xi - \frac{\kappa}{2}\var x^\top\xi \\
        & \mathrm{subject\,to} & & x^\top\one = 1\\\\
    \end{aligned}
\end{equation}
\begin{equation}
    \label{problem:LinearProgram}
    \begin{aligned}
        & \underset{x\in\R^J,\;x\geq 0}{\mathrm{minimize}} & & \mathcal{R}_{\alpha,\gamma}(-x^\top\xi)\; \\
        & \mathrm{subject\,to} & & \E x^\top\xi \geq \mu \\
        & & & x^\top\one = 1
    \end{aligned}
\end{equation}
with the risk measure $\mathcal{R}_{\alpha,\gamma}(X) = (1-\gamma)\E X + \gamma\AVaR_\alpha(X)$ for $\alpha,\gamma\in(0,1)$ and a real-valued random variable $X\colon\Omega\to\R$ defined on the probability space $(\Omega, \Sigma, \pr)$. \medskip

These models have different strengths and weaknesses. A model outcome may be very vulnerable to small changes in serval input parameters and cause unstable results. Others are less sensitive and are necessary for a robust model. We are going to highlight the respective sensitivities of each model given different parameters. Therefore, we give a mathematical approach to measure sensitivity in section \ref{section:SensitivityAnalysis}. Afterwards, we analyze the models (\ref{problem:Markowitz}), (\ref{problem:UtilityMaximization}) and (\ref{problem:LinearProgram}). A numerical approach to risk and the solutions of these three problems is given in section \ref{section:MathematicalModels}. To this end, we reuse the stock data from the lecture notes \cite{PichlerPortfoliooptimierung}, to allow better comparability of the models and results. In addition, in section \ref{section:SimulationOfStocks} we derive an algorithm to simulate stock data.

\begin{figure}[h]
  \centering
  \includesvg[width=\textwidth]{assets/plotStocksNew}
  \caption{Stock prices from \cite[p.~18]{PichlerPortfoliooptimierung}}
\end{figure}
\section{Preliminaries}\label{section:Preliminaries}
To obtain the present value $PV$ from a future value $FV$ at time $t>0$ we use the well-known formula $PV = FV e^{-\delta t}$ for some $\delta\in\mathbb{R}$, cf. \citet[p.~21]{PichlerStochastischeFinanzmaerkte}. Hence, the force of interest reads
    \begin{align*}
        \delta = \frac{1}{t}\ln\left(\frac{FV}{PV}\right).
    \end{align*}
In the same way, we design the returns of our assets, cf. \citet[pp.~19-20]{PichlerPortfoliooptimierung}. For timestamps $\{t_0,\ldots,t_n\}$, the return per time period $[t_{i-1}, t_i]$ of the asset $j\in\{1,\ldots,J\}$ is given by
\begin{equation*}
    \xi_i^j := \frac{1}{t_i-t_{i-1}}\ln\left(\frac{S_{t_i}^j}{S_{t_{i-1}}^j}\right).
\end{equation*}
We store these stock observations in a matrix
\begin{equation*}
    \Xi =
    \begin{pmatrix}
        \xi_1^1 & \cdots & \xi_1^J \\
        \vdots  & \ddots & \vdots  \\
        \xi_n^1 & \cdots & \xi_n^J
    \end{pmatrix}\in\R^{n\times J}
\end{equation*}
We introduce a probability measure $\pr(\xi = e_i^\top\Xi)\in[0,1]$ for a random variable $\xi\colon\Omega\to\R^J$ and a probability space $(\Omega,\Sigma,\pr)$ and a measurable space with Borel $\sigma$-algebra $(\R^J,\mathcal{B}(\R^J))$ again. We will store these probabilities for every stock $j\in\{1,\ldots,J\}$ in the probability vector
\begin{equation*}
    p =
    \begin{pmatrix}
        p_1 \\
        \vdots \\
        p_n
    \end{pmatrix} =
    \begin{pmatrix}
        \pr(\xi = e_1^\top\Xi) \\
        \vdots \\
        \pr(\xi = e_n^\top\Xi)
    \end{pmatrix} =
    \begin{pmatrix}
        \frac{t_1-t_0}{t_n-t_0} \\
        \vdots \\
        \frac{t_n-t_{n-1}}{t_n-t_0}
    \end{pmatrix}
\end{equation*}
with property $\sum_{i=1}^n\pr(\xi = e_i^\top\Xi) = \sum_{i=1}^n p_i = 1$. Then we obtain after canceling another telescoping sum the expected return for every stock and store them in a vector $r = \E\xi = p^\top\Xi \in\R^J$,
\begin{equation*}
    \begin{aligned}
    r_j 
    &= \E\xi^j 
    = \sum_{i=1}^n p_i\xi_i^j
    = \sum_{i=1}^n \frac{t_i-t_{i-1}}{t_n-t_0}\frac{1}{t_i-t_{i-1}}\ln\left(\frac{S_{t_i}^j}{S_{t_{i-1}}^j}\right)
    = \frac{1}{t_n-t_0}\sum_{i=1}^n\left(\ln(S_{t_i}^j)-\ln(S_{t_{i-1}}^j)\right) \\
    &= \frac{1}{t_n-t_0}\ln\left(\frac{S_{t_n}^j}{S_{t_0}^j}\right).
    \end{aligned}
\end{equation*}
For the rest of section \ref{section:Preliminaries} Preliminaries cf. \citet[pp.~20,21,35,44]{PichlerPortfoliooptimierung}.
\begin{lemma}
\label{lemma:ExpectationVariance}
    Let $x\in\R^J$ be an arbitrary allocation. Then
    \begin{enumerate}
        \item $\E x^\top\xi = x^\top r$ and
        \item $\var x^\top\xi = x^\top\Sigma x$.
    \end{enumerate}
\end{lemma}\newpage
\begin{proof}\mbox{}
    \begin{enumerate}[\itshape(i)]
        \item $\E x^\top\xi 
                = \sum_{i=1}^np_i\left(\sum_{j=1}^J\xi_i^jx_j\right)
                = p^\top\Xi x
                = r^\top x
                = x^\top r$ and
        \item $\var x^\top\xi
                = \E(x^\top\xi-\E x^\top\xi)^2
                = \E(x^\top\xi)^2 - (\E x^\top\xi)^2
                = \sum_{i=1}^np_i\left(\sum_{i=1}^J\xi_i^jx_j\right)^2 - \left(\sum_{i=1}^np_i\xi_i^jx_j\right)^2 \\
                = \sum_{i=1}^np_i\left(\sum_{j=1}^J\sum_{j^\prime=1}^J\xi_i^jx_j\cdot\xi_i^{j^\prime}x_{j^\prime}\right) - \sum_{i=1}^np_i\left(\sum_{j=1}^J\xi_i^jx_j\right)\cdot\sum_{i^\prime=1}^np_{i^\prime}\left(\sum_{j^\prime=1}^J\xi_{i^\prime}^{j^\prime}x_{j^\prime}\right) \\
                = \sum_{j=1}^Jx_j\sum_{j^\prime=1}^Jx_{j^\prime}\cdot\sum_{i=1}^np_i\xi_i^j\xi_i^{j^\prime} - \sum_{j=1}^Jx_j\sum_{j^\prime=1}^Jx_{j^\prime}\cdot\sum_{i=1}^np_i\xi_i^j\sum_{i^\prime=1}^np_{i^\prime}\xi_{i^\prime}^{j^\prime} \\
                = \sum_{j=1}^Jx_j\sum_{j^\prime=1}^Jx_{j^\prime}\cdot\left(\sum_{i=1}^np_i\xi_i^j\xi_i^{j^\prime}-\sum_{i=1}^np_i\xi_i^j\cdot\sum_{i^\prime=1}^np_{i^\prime}\xi_{i^\prime}^{j^\prime}\right) \\
                = \sum_{j=1}^Jx_j\sum_{j^\prime=1}^Jx_{j^\prime}\cdot\Sigma_{jj^\prime} = x^\top\Sigma x$.
    \end{enumerate}
\end{proof}
\begin{remark}
    The Markowitz optimization problem (\ref{problem:Markowitz}) now reads
    \begin{equation}
    \label{problem:MarkowitzNew}
        \begin{aligned}
            & \underset{x\in\R^J}{\mathrm{minimize}} & & x^\top\Sigma x \\
            & \mathrm{subject\,to} & & x^\top r \geq \mu \\
            & & & x^\top\one = 1
        \end{aligned}
    \end{equation}
    and the Utility Maximization problem (\ref{problem:UtilityMaximization}) is equivalent to
    \begin{equation}
    \label{problem:UtilityMaximizationNew}
        \begin{aligned}
            & \underset{x\in\R^J}{\mathrm{maximize}} & & x^\top r - \frac{\kappa}{2}x^\top\Sigma x \\
            & \mathrm{subject\,to} & & x^\top\one = 1.
        \end{aligned}
    \end{equation}
\end{remark}
\begin{definition}
Let $X\colon\Omega\to\R$ be a real-valued random variable defined on the probability space $(\Omega,\Sigma,\Pr)$. Further, we denote the cumulative distribution function of $X$ by $F_X(x)=\Pr(X\leq x)$. Then we define for an $\alpha\in(0,1)$
    \begin{enumerate}[\itshape(i)]
        \item the \emph{Value at Risk at level} $\alpha$ or\emph{ quantile function}
        \begin{equation*}
            \VaR_\alpha(X) = F_X^{-1}(\alpha)
        \end{equation*}
        \item and the \emph{Average Value at Risk at level} $\alpha$ or \emph{expected shortfall}
        \begin{equation*}
            \AVaR_\alpha(X) = \frac{1}{1-\alpha}\int_\alpha^1\VaR_{\alpha^\prime}(X)\,\mathrm{d}\alpha^\prime.
        \end{equation*}
    \end{enumerate}
\end{definition}
\begin{remark}
    For $\alpha\in\{0,1\}$ we have
    \begin{enumerate}[\itshape(i)]
        \item $\VaR_0(X) = -\infty$ and $\VaR_1(X) = \infty$ as well
        \item $\AVaR_0(X) = \E X$ and $\AVaR_1(X) = \esssup X$.
    \end{enumerate}
\end{remark}
\begin{lemma}\label{lemma:AVaR} Let $X\colon\Omega\to\R$ be a real-valued random variable defined on the probability space $(\Omega,\Sigma,\Pr)$. We denote the positive part of a function by $(\cdot)^+=\max\{0,\cdot\}$. Then for all $\alpha\in(0,1)$
    \begin{equation*}
        \AVaR_\alpha(X) = \inf_{q\in\R}q+\frac{1}{1-\alpha}\E(X-q)^+.
    \end{equation*}
\end{lemma}
\begin{proof}
    From first-order optimality condition we receive
    \begin{equation*}
        \begin{aligned}
            0 &\overset{!}{=} \frac{\mathrm{d}}{\mathrm{d}q}\left(q+\frac{1}{1-\alpha}\E(X-q)^+\right)
            = 1 - \frac{1}{1-\alpha}\E\one_{\{X>q\}} \\
            &= 1 - \frac{1}{1-\alpha}\Pr(X>q)
            = 1 - \frac{1}{1-\alpha}(1-\Pr(X\leq q))
        \end{aligned}
    \end{equation*}
    and the optimal solution is $q^* = q = F_X^{-1}(\alpha) = \VaR_\alpha(X)$. Hence, we get the result
    \begin{equation*}
        \begin{aligned}
            q^* + \frac{1}{1-\alpha}\E(X-q^*)^+
            &= \VaR_\alpha(X) + \frac{1}{1-\alpha}\int_0^1(\VaR_{\alpha^\prime}(X)-\VaR_\alpha(X))^+\,\mathrm{d}\alpha^\prime \\
            &= \VaR_\alpha(X) + \frac{1}{1-\alpha}\int_\alpha^1\VaR_{\alpha^\prime}(X)-\VaR_\alpha(X)\,\mathrm{d}\alpha^\prime \\
            &= \frac{1}{1-\alpha}\int_\alpha^1\VaR_{\alpha^\prime}(X)\,\mathrm{d}\alpha^\prime
            = \AVaR_\alpha(X).
        \end{aligned}
    \end{equation*}
\end{proof}
\section{Mathematical Models}\label{section:MathematicalModels}
For section \ref{section:MathematicalModels} Mathematical Models cf. \citet[pp.~23,24,27,32,52]{PichlerPortfoliooptimierung}.
\subsection{Markowitz Model}
\begin{theorem}
    Let $r = \E\xi \in\R^J$ be the expected return of the portfolio and $\mu\geq0$ be a lower bound of the expected return $\E x^\top\xi\in\R$ to an allocation $x\in\R^J$. Then the optimization problem
    \begin{equation*}
        \begin{aligned}
            & \underset{x\in\R^J}{\mathrm{minimize}} & & x^\top\Sigma x \\
            & \mathrm{subject\,to} & & x^\top r \geq \mu \\
            & & & x^\top\one = 1
        \end{aligned}
    \end{equation*}
    has an explicit solution $x^*\colon\R_{\geq 0}\to\R^J$ defined by
    \begin{equation}
    \label{eq:markowitz-solution}
        \begin{aligned}
            x^*(\mu) = \lambda_1^*(\mu)\precision r + \lambda_2^*(\mu)\precision\one
        \end{aligned}
    \end{equation}
    with optimal shadow prices at
    \begin{equation*}
        \begin{aligned}
            & \lambda_1^*(\mu) = \frac{\mu(\one^\top\precision\one) - (r^\top\precision\one)}{(r^\top\precision r)(\one^\top\precision\one) - (r^\top\precision\one)^2}\\
            & \lambda_2^*(\mu) = \frac{(r^\top\precision r) - \mu(r^\top\precision\one)}{(r^\top\precision r)(\one^\top\precision\one) - (r^\top\precision\one)^2}.
        \end{aligned}
    \end{equation*}
\end{theorem}
\begin{proof}
    From the objective and constraints, we receive the Lagrangian\footnote{Joseph-Louis Lagrange (1736-1813) was an Italian and naturalized French mathematician, physicist and astronomer}and its partial derivatives
    \begin{equation*}
        \begin{aligned}
            L(x,\lambda_1,\lambda_2)
        &= \frac{1}{2}x^\top\Sigma x
        + \lambda_1\left(\mu-x^\top r\right)
        + \lambda_2\left(1-x^\top\one\right) \\\\
        0 &\overset{!}{=} \frac{\partial L}{\partial x}
        = \Sigma x - \lambda_1r - \lambda_2\one\\
        0 &\overset{!}{=} \frac{\partial L}{\partial\lambda_1}
        = \mu-x^\top r\\
        0 &\overset{!}{=} \frac{\partial L}{\partial\lambda_2}
        = 1-x^\top\one
        \end{aligned}
    \end{equation*}
    with shadow prices $\lambda_1,\lambda_2\in\R$. Hence, from the optimal condition, we get
    \begin{equation*}
        \begin{aligned}
            x^* &= \precision(\lambda_1^*r + \lambda_2^*\one)
            = \lambda_1^*\precision r + \lambda_2^*\precision\one \\\\
            \mu &= (x^*)^\top r = r^\top x^* = \lambda_1^*r^\top\precision r + \lambda_2^*r^\top\precision\one \\
            1 &= (x^*)^\top\one = \one^\top x^* = \lambda_1^*\one^\top\precision r + \lambda_2^*\one^\top\precision\one
        \end{aligned}
    \end{equation*}
    or rewritten in a matrix equation
    \begin{equation*}
        \begin{pmatrix}
            \A & \B \\
            \B & \C
        \end{pmatrix}
        \begin{pmatrix}
            \lambda_1^* \\
            \lambda_2^*
        \end{pmatrix} =
        \begin{pmatrix}
            \mu \\
            1
        \end{pmatrix}
    \end{equation*}
    with solutions 
    \begin{equation*}
        \begin{aligned}
            \lambda_1^* &= \lambda_1^*(\mu) = \frac{\mu(\C)-(\B)}{(\A)(\C)-(\B)^2}\;\text{and} \\
            \lambda_2^* &= \lambda_2^*(\mu) = \frac{(\A)-\mu(\B)}{(\A)(\C)-(\B)^2}.
        \end{aligned}
    \end{equation*}
    Hence, the optimal allocation solution is now obtained with
    \begin{equation*}
        \begin{aligned}
            x^* = x^*(\mu) = \lambda_1^*(\mu)\precision r + \lambda_2^*(\mu)\precision\one.
        \end{aligned}
    \end{equation*}
\end{proof}
\begin{corollary}
\label{cor:AffineFunction}
    The components of solution (\ref{eq:markowitz-solution}) are affine functions.
\end{corollary}
\begin{proof}
    We can reorder (\ref{eq:markowitz-solution}) and receive the statement
    \begin{equation}
    \label{eq:AffineFunction}
        \begin{aligned}
            x^*(\mu) &=
            \lambda_1^*(\mu)\precision r + \lambda_2^*(\mu)\precision\one \\
            &= \frac{\mu(\C)-(\B)}{(\A)(\C)-(\B)^2}\precision r + \frac{(\A)-\mu(\B)}{(\A)(\C)-(\B)^2}\precision\one \\
            &= \mu\underbrace{\frac{(\C)\precision r - (\B)\precision\one}{\D}}_{\text{slope vector}} + \underbrace{\frac{(\A)\precision\one - (\B)\precision r}{\D}}_{\text{intercept vector}}.
        \end{aligned}
    \end{equation}
\end{proof}
\begin{corollary}\emph{(Two fund Theorem)}
\label{cor:TwoFundTheorem}
    Let $x_1^* = x^*(\mu_1)\in\R^J$ and $x_2^* = x^*(\mu_2)\in\R^J$ be two solutions of (\ref{problem:Markowitz}) for different $\mu_1,\mu_2\geq0$, i.e. $\mu_1\neq\mu_2$. Then every affine combination of these two allocations is also a solution of (\ref{problem:Markowitz}).
\end{corollary}
\begin{proof}
     Since Corollary \ref{cor:AffineFunction} the components of (\ref{eq:markowitz-solution}) are affine functions. So, let $u\in\R^J$ denote the slope vector and let $v\in\R^J$ be the intercept vector of the optimal solution. Now we can write $x_1^*=\mu_1u+v$ and $x_2^*=\mu_2u+v$. Then the affine combination of $x_1^*$ and $x_2^*$
    \begin{equation*}
        \begin{aligned}
            (1-\theta)x_1^* + \theta x_2^*
            = (1-\theta)(\mu_1u+v) + \theta(\mu_2u+v)
            = \underbrace{((1-\theta)\mu_1 + \theta\mu_2)}_{\in\R}u + \underbrace{((1-\theta)+\theta)}_{=1}v
        \end{aligned}
    \end{equation*}
    for an arbitrary $\theta\in\R$, is indeed a solution of (\ref{problem:Markowitz}) as well.
\end{proof}
\begin{remark}
    In Figure \ref{fig:AllocationMarkowitz} we see the solutions of the Markowitz model (\ref{problem:Markowitz}) for varying minimum return $\mu\geq0$. The components of the allocation vector $x\in\R^J$ are affine functions, and the affine combination of two arbitrary allocations is also an allocation according to the Markowitz model. So, we look at the visualization of the statements of Corollary \ref{cor:AffineFunction} and Corollary \ref{cor:TwoFundTheorem} (Two fund Theorem). The dotted line represents the minimum risk portfolio, i.e.\,the minimum variance portfolio at $\mu=\frac{\B}{\C}$.
\end{remark}
\begin{figure}[h]
  \centering
  \includesvg[width=\textwidth]{assets/plotAllocationMarkowitzNew}
  \caption{Asset allocation according to Markowitz model (\ref{problem:Markowitz}), cf. \citet[p.~27]{PichlerPortfoliooptimierung}}
\label{fig:AllocationMarkowitz}
\end{figure}
\newpage
\subsection{Utility Maximization}
\begin{theorem}
    Let $r = \E\xi \in\R^J$ be the expected return of the portfolio and $\kappa>0$ be a parameter of risk aversion. Then the optimization problem
    \begin{equation*}
        \begin{aligned}
            & \underset{x\in\R^J}{\mathrm{maximize}} & & x^\top r - \frac{\kappa}{2}x^\top\Sigma x \\
            & \mathrm{subject\,to} & & x^\top\one = 1
        \end{aligned}
    \end{equation*}
    has an explicit solution $x^*\colon\R_{>0}\to\R^J$ defined by
    \begin{equation}
    \label{eq:utility-maximization}
        \begin{aligned}
            x^*(\kappa) = \frac{1}{\kappa}\precision\left(r+\frac{\kappa-r^\top\precision\one}{\one^\top\precision\one}\one\right).
        \end{aligned}
    \end{equation}
\end{theorem}
\begin{proof}
    From the objective and constraints, we receive the Lagrangian and its partial derivatives
    \begin{equation*}
        \begin{aligned}
            L(x,\lambda) &= x^\top r - \frac{\kappa}{2}x^\top\Sigma x + \lambda(x^\top\one - 1) \\\\
            0 &\overset{!}{=} \frac{\partial L}{\partial x} = r - \kappa\Sigma x + \lambda\one \\
            0 &\overset{!}{=} \frac{\partial L}{\partial \lambda} = x^\top\one - 1
        \end{aligned}
    \end{equation*}
    with Lagrange multiplier or shadow price $\lambda\in\R$. Hence, from the optimal condition, we get
    \begin{equation*}
        \begin{aligned}
            x^* &= \frac{1}{\kappa}\precision(r+\lambda^*\one) \\
            1 &= (x^*)^\top\one = \one^\top x^* \\
            &= \frac{1}{\kappa}\one^\top\precision r + \frac{\lambda^*}{\kappa}\one^\top\precision\one
        \end{aligned}
    \end{equation*}
    and after rearrangement $\lambda^*=\frac{\kappa-\one^\top\precision r}{\C}=\frac{\kappa-\B}{\C}$. Then we obtain the optimal solution
    \begin{equation*}
        x^* = x^*(\kappa) = \frac{1}{\kappa}\precision\left(r+\frac{\kappa-\B}{\C}\one\right).
    \end{equation*}
\end{proof}
\begin{remark}
    In Figure \ref{fig:AllocationUtilityMaximization} we see the solutions of the Utility Maximization model (\ref{problem:UtilityMaximization}) to growing risk aversions $\kappa>0$. The components of the allocation vector $x\in\R^J$ converge to a minimal risk portfolio for an increasing risk aversion $\kappa\to\infty$. Hence, we also visualize in Figure \ref{fig:AllocationUtilityMaximization} the statement we will see in Corollary \ref{cor:UtilityMaximization} \textit{(ii)}.
\end{remark}
\begin{figure}[h]
  \centering
  \includesvg[width=\textwidth]{assets/plotAllocationUtilityMaximizationNew}
  \caption{Asset allocation according to model (\ref{problem:UtilityMaximization})}
\label{fig:AllocationUtilityMaximization}
\end{figure}\newpage
\begin{corollary}
\label{cor:UtilityMaximization}
    The following statements hold true.
    \begin{enumerate}
        \item The solutions (\ref{eq:markowitz-solution}) and (\ref{eq:utility-maximization}) coincide for $\kappa = \frac{\D}{\mu(\C) - (\B)}$ and
        \item The solution (\ref{eq:utility-maximization}) converges to an allocation $x^*=\frac{1}{\C}\precision\one$ for $\kappa\to\infty$ with
        \item Convergence rate $x^*(\kappa)-x^*=\mathcal{O}(\frac{1}{\kappa})$.
    \end{enumerate}
\end{corollary}
\begin{proof}\mbox{}
    \begin{enumerate}[\itshape(i)]
        \item Let us have a look at the solutions (\ref{eq:markowitz-solution}) and (\ref{eq:utility-maximization})
        \begin{equation*}
            \begin{aligned}
                x^* &= \lambda_1^*(\mu)\precision r + \lambda_2^*(\mu)\precision\one \\
                x^* &= \frac{1}{\kappa}\precision r + \frac{\kappa-(\B)} {\kappa(\C)}\precision\one.
            \end{aligned}
        \end{equation*}
        Comparing the coefficients delivers $\lambda_1^*(\mu) \overset{!}{=} \frac{1}{\kappa}$. Hence, the statement $\kappa = \lambda_1^*(\mu)^{-1}$ follows.
        \item The Statement follows directly by rewriting the solution
        \begin{equation*}
            x^*(\kappa) 
            = \frac{1}{\kappa}\precision r + \frac{\kappa-(\B)}{\kappa(\C)}\precision\one
            = \frac{1}{\kappa}\precision r + \frac{1}{\C}\left(\precision\one - \frac{\B}{\kappa}\precision\one\right)
        \end{equation*}
        and push it to the limit $x^* = \lim_{\kappa\to\infty}x^*(\kappa) = \frac{1}{\C}\precision\one$.
        \item The error can be estimated by
        \begin{equation*}
            \begin{aligned}
                \Vert x^*(\kappa)-x^*\Vert
                &=\Vert x^*(\kappa)-\lim_{\kappa\to\infty}x^*(\kappa)\Vert\\
                &= \left\Vert\frac{1}{\kappa}\precision r+\frac{1}{\C}\precision\one - \frac{\B}{\kappa(\C)}\precision\one -\frac{1}{\C}\precision\one\right\Vert \\
                &= \left\Vert\frac{1}{\kappa}\precision\left(r - \frac{\B}{\C}\one\right)\right\Vert
                \leq \frac{1}{\kappa}\left\Vert\precision\left(r - \frac{\B}{\C}\one\right)\right\Vert.
            \end{aligned}
        \end{equation*}
        Since the latter norm remains constant, the convergence rate is $x^*(\kappa)-x^*=\mathcal{O}(\frac{1}{\kappa})$.
    \end{enumerate}
    Hence, the allocation converges for sufficiently large and growing risk aversion $\kappa\gg0$.
\end{proof}
\subsection{Linear Programming}
\begin{theorem}
    The optimization problem (\ref{problem:LinearProgram}) can be formulated as a linear program of the form
    \begin{equation}
    \label{problem:LinearProgrammNew}
        \begin{aligned}
        & \underset{x^\prime}{\mathrm{minimize}} & & c^\top x^\prime\\
        & \mathrm{subject\,to} & & A_1x^\prime\leq b_1 \\
        & & & A_2x^\prime=b_2.
        \end{aligned}
    \end{equation}
\end{theorem}
\begin{proof}
    Recall the risk measure $\mathcal{R}_{\alpha,\gamma}(X)=(1-\gamma)\E X + \gamma\AVaR_\alpha(X)$ for a real-valued random variable $X\colon\Omega\to\R$ defined on the probability space $(\Omega,\Sigma,\Pr)$. Using Lemma \ref{lemma:AVaR} the objective function of the minimization problem (\ref{problem:LinearProgram}) reads
    \begin{equation*}
        \begin{aligned}
            \mathcal{R}_{\alpha,\gamma}(-x^\top\xi)
        &=(1-\gamma)\E(-x^\top\xi) + \gamma\AVaR_\alpha(-x^\top\xi) \\
        &=-(1-\gamma)\E x^\top\xi + \gamma\left(\inf_{q\in\R}q+\frac{1}{1-\alpha}\E(-x^\top\xi-q)^+\right).
        \end{aligned}
    \end{equation*}
    To achieve a linear objective, we estimate the $(\cdot)^+=\max\{0,\cdot\}$ with slack-variables $z_1,\ldots,z_n\geq0$ the following way
    \begin{equation*}
        \begin{aligned}
            0 &\leq -x^\top\xi_1-q \overset{!}{\leq} z_1 \\
            &\;\;\vdots\;\;\;\;\;\;\;\;\;\;\;\;\;\;\;\;\;\;\vdots \\
            0&\leq -x^\top\xi_n-q \overset{!}{\leq} z_n.
        \end{aligned}
    \end{equation*}
    With the result $\E x^\top\xi = x^\top r$ from Lemma \ref{lemma:ExpectationVariance} the optimization problem reads as follows
    \begin{equation*}
        \begin{aligned}
        & \underset{q\in\R;\,x,z \geq 0}{\mathrm{minimize}} & & -(1-\gamma) x^\top r+\gamma q+\gamma\frac{1}{1-\alpha}p^\top z\\
        & \mathrm{subject\,to} & & -x^\top r\leq-\mu \\
        & & & -x^\top\xi_i-q-z_i\leq 0 \\
        & & & \;\;\;\,x^\top\one = 1
        \end{aligned}
    \end{equation*}
    for $i=1,\ldots,n$. Or rewritten with $x^\prime = 
    \begin{pmatrix}
        x & q & z
    \end{pmatrix}^\top=
    \begin{pmatrix}
        x_1 & \cdots & x_J & q & z_1 & \cdots & z_n
    \end{pmatrix}^\top$
    \begin{equation}\label{problem:LinearProgrammFullMatrix}
        \begin{aligned}
        & \underset{q\in\R;\,x,z \geq 0}{\mathrm{minimize}} & & 
        \begin{pmatrix}
            -(1-\gamma) r^\top & \gamma & \gamma\frac{1}{1-\alpha}p^\top
        \end{pmatrix}
        \begin{pmatrix}
            x \\
            q \\
            z
        \end{pmatrix}\\
        & \mathrm{subject\,to} & &
        \begin{pmatrix}
            -r_1 & \cdots & -r_J & 0 & 0 & \cdots & 0 \\
            -\xi_1^1 & \cdots & -\xi_1^J &-1&-1& \cdots &0 \\
            \vdots & \ddots & \vdots & \vdots & \vdots & \ddots & \vdots \\
            -\xi_n^1 & \cdots & -\xi_n^J & -1 &0& \cdots & -1
        \end{pmatrix}
        \begin{pmatrix}
            x \\
            q \\
            z
        \end{pmatrix}\leq
        \begin{pmatrix}
            -\mu \\
            0 \\
            \vdots \\
            0
        \end{pmatrix}\\
        & \mathrm{and} & &
        \begin{pmatrix}
            1 & \cdots & 1 & 0 & 0 & \cdots & 0
        \end{pmatrix}
        \begin{pmatrix}
            x \\
            q \\
            z
        \end{pmatrix} = 1.
        \end{aligned}
    \end{equation}
    Hence, problem (\ref{problem:LinearProgrammFullMatrix}) is a linear program of the form (\ref{problem:LinearProgrammNew}).
\end{proof}
\begin{remark}
    The problem (\ref{problem:LinearProgrammNew}) is well-known and can be solved e.g.\,using the Python package \texttt{scipy} by running the static method $\texttt{scipy.optimize.linprog(c}=c, \texttt{A}_\texttt{ub}=A_1, \texttt{b}_\texttt{ub}=b_1, \\ \texttt{A}_\texttt{eq}=A_2, \texttt{b}_\texttt{eq}=b_2, \texttt{bounds}=\texttt{[(}0,\texttt{None)},\texttt{(None},\texttt{None)},\texttt{(}0,\texttt{None)]}, \texttt{method}=\texttt{"highs")}$.
\end{remark}
\begin{claim}
    The components of the allocation $x\in\R^J$ due to (\ref{problem:LinearProgrammFullMatrix}) are piecewise affine functions.
\end{claim}
\begin{figure}[h]
  \centering
  \includesvg[width=\textwidth]{assets/plotAllocationLinearProgrammingNew}
  \caption{Asset allocation according to model (\ref{problem:LinearProgram})}
\end{figure}\newpage
\section{Simulation of Stocks}\label{section:SimulationOfStocks}
Let $\mu\in\R$ describe the drift and $\sigma\geq0$ represent the volatility of our stock. To simulate a single stock in numerical analysis, we use the Geometric Brownian Motion. It is the solution of the stochastic differential equation
\begin{equation*}
    dS_t = \mu S_tdt + \sigma S_tdW_t
\end{equation*}
where $W_t$ is a Wiener process, also known as Brownian Motion. The equation has the solution
\begin{equation*}
    S_t = S_0\exp\left(\left(\mu-\frac{\sigma^2}{2}\right)t+\sigma W_t\right)
\end{equation*}
for some start stock value $S_0\in\R_{\geq0}$, cf. \citet[p.~113]{PichlerStochastischeFinanzmaerkte}. This leads to the following algorithm:
\RestyleAlgo{ruled}
\SetKwComment{Comment}{// }{}
\begin{algorithm}
    \caption{Geometric Brownian Motion}
    \label{algo:GBM}
    \KwData{$s_0\in\R_{\geq0}, \mu\in\R, \sigma\geq0$}
    $S_0 \gets s_0$\;
    $i \gets 1$\;
    \While{$i \leq n$}{
        $N_i \gets N\sim\mathcal{N}(0,p_i)$ \Comment*[r]{$N\sim\mathcal{N}\left(\mu^\prime, \sigma^{\prime2}\right)$ is normally distributed}
        $S_i \gets S_{i-1}\exp\left(\left(\mu-\frac{\sigma^2}{2}\right)p_i + \sigma N_i\right)$\;
        $i \gets i+1$\;
    }
    \KwResult{$S\in\R_{\geq0}^n$}
\end{algorithm}

\section{Sensitivity analysis}\label{section:SensitivityAnalysis}
In this chapter, we investigate how sensitive outputs act by small changes for an arbitrary input parameter $\theta\in T$, e.g. $\theta=\mu\geq 0$ or $\theta=\kappa > 0$. First, we define the following Sensitivity operators
\begin{definition}
Let $X^T=\{f\mid f\colon T\to X\,\text{function}\}$ be the space of functions $f\colon T\to X$ with $T$ a feasible set of a model input parameter and $X\subseteq\R^J$. Then we call
\begin{enumerate}[\itshape(i)]
    \item $\mathcal{S}\colon T\to X^T$ given by
    \begin{equation*}
        \mathcal{S}(\theta)
        = \frac{\partial x^*}{\partial\theta}
    \end{equation*}
    the \emph{absolute Sensitivity} of the numerical model with an optimal solution $x^*$ and
    \item $\mathcal{E}\colon T\to X^T$ given by
    \begin{equation*}
        \mathcal{E}(\theta) = \frac{\partial\ln(x^*)}{\partial\ln(\theta)}
    \end{equation*}
    the \emph{relative Sensitivity} or \emph{Elasticity} of the numerical model with an optimal solution $x^*$.
\end{enumerate}
\end{definition}
\begin{lemma}
For all parameters $\theta\in T$ holds
    \begin{equation*}
        \mathcal{E}(\theta) = \frac{\theta}{x^*}\mathcal{S}(\theta).
    \end{equation*}
\end{lemma}
\begin{proof}
We will use the derivative of the logarithm $\frac{\mathrm{d}\ln(x)}{\mathrm{d}x}=\frac{1}{x}$ and apply the chain rule
    \begin{equation*}
        \mathcal{E}(\theta) 
        = \frac{\partial\ln(x^*)}{\partial\ln(\theta)}
        = \frac{\partial\ln(x^*)}{\partial\theta}\cdot\frac{\partial\theta}{\partial\ln(\theta)}
        = \frac{1}{x^*}\frac{\partial x^*}{\partial\theta}\cdot\theta
        = \frac{\theta}{x^*}\frac{\partial x^*}{\partial\theta}.
    \end{equation*}
    Hence, we receive the result $\mathcal{E}(\theta)=\frac{\theta}{x^*}\mathcal{S}(\theta)$.
\end{proof}
\begin{lemma}
For all parameters $\theta\in T$ holds
    \begin{equation*}
        \mathcal{E}(\theta) = \lim_{\Delta\theta\to 0}\frac{\frac{\Delta x^*}{x^*}}{\frac{\Delta\theta}{\theta}}.
    \end{equation*}
\end{lemma}
\begin{proof}
    It holds
    \begin{equation*}
        \mathcal{E}(\theta)
        = \frac{\theta}{x^*}\mathcal{S}(\theta)
        = \frac{\theta}{x^*}\frac{\partial x^*}{\partial\theta}
        = \frac{\theta}{x^*}\lim_{\Delta\theta\to 0}\frac{\Delta x^*}{\Delta\theta}
        = \lim_{\Delta\theta\to 0}\frac{\frac{\Delta x^*}{x^*}}{\frac{\Delta\theta}{\theta}}.
    \end{equation*}
\end{proof}
\begin{remark}
    We can determine Sensitivity and Elasticity numerically by $\mathcal{S}(\theta) \approx \frac{\Delta x^*}{\Delta\theta}$ and $\mathcal{E}(\theta) \approx \frac{\Delta x^*}{x^*}/\frac{\Delta\theta}{\theta}$ respectively for sufficient small $\Vert\Delta\theta\Vert\ll 1$.
\end{remark}

\subsection{Markowitz model}
\begin{theorem}
\label{thm:MarkowitzSensitivity}
    The sensitivity of (\ref{eq:markowitz-solution}) with respect to $\mu$ is given by
    \begin{equation*}
        \mathcal{S}(\mu) = \frac{\C}{\D}\precision r - \frac{\B}{\D}\precision\one.
    \end{equation*}
\end{theorem}
\begin{proof}
    We reuse equation (\ref{eq:AffineFunction}) in the proof of Corollary \ref{cor:AffineFunction} and differentiate the solution
    \begin{equation*}
        \begin{aligned}
            x^*(\mu) 
            &= \mu\frac{(\C)\precision r - (\B)\precision\one}{\D} + \frac{(\A)\precision\one - (\B)\precision r}{\D} \\
            \frac{\partial x^*}{\partial\mu}
            &= \frac{\C}{\D}\precision r - \frac{\B}{\D}\precision\one.
        \end{aligned}
    \end{equation*}
\end{proof}
\begin{remark}
    We can conclude from Theorem \ref{thm:MarkowitzSensitivity} that the sensitivity with respect to $\mu$ of the Markowitz model (\ref{problem:Markowitz}) depends only on the covariance matrix $\Sigma^{-1}$ and the expected return $r$. Furthermore, the sensitivity $\mathcal{S}(\mu)=\mathrm{const.}$ remains constant for all minimum returns $\mu\geq0$.
\end{remark}
\newpage
\begin{remark}
    Figure \ref{fig:plotSensitivityElasticityMarkowitz} shows the sensitivity and elasticity with respect to $\mu$ of the Markowitz model (\ref{problem:Markowitz}). The sensitivity $\mathcal{S}(\mu)=\text{const.}$ remains constant, as shown in Theorem \ref{thm:MarkowitzSensitivity} previously. However, the elasticity increases to its maximum $\max_{\mu\geq0}\mathcal{E}(\mu)\approx 1.5$ at $\mu\approx25\,\%$ and shrinks to its limit $\mathcal{E}(\mu)\searrow 1$ for $\mu\gg25\,\%$. Hence, the Markowitz model is an elastic model for a sufficiently great minimum return.
\end{remark}
\begin{figure}[h]
  \centering
  \includesvg[width=\linewidth]{assets/plotSensitivityElasticityMarkowitzNew}
  \caption{Sensitivity and Elasticity of the Markowitz model (\ref{problem:Markowitz}) for varying minimum return $\mu$}
  \label{fig:plotSensitivityElasticityMarkowitz}
\end{figure}
\begin{claim}
    The Markowitz model (\ref{problem:Markowitz}) is highly sensitive with respect to stock prices $S$.
\end{claim}
\begin{remark} \label{remark:meanVariancePlotMarkowitz}
    Figure \ref{fig:plotMeanVarianceMarkowitz} shows the mean-variance plot of the Markowitz portfolio according to problem (\ref{problem:Markowitz}). On the one hand, we can see the efficient frontier for stock prices $S$ taken from \cite[p.~18]{PichlerPortfoliooptimierung} and, on the other hand, there is a scatter plot of optimal Markowitz portfolio allocations with noised stock prices $S^\varepsilon$. Noise is added as follows: $S^\varepsilon_i=S_i+\varepsilon\bar{S}N_i$ for $i=0,\ldots,n$ with sample mean $\bar{S} = \frac{1}{n+1}\sum_{i=0}^nS_i$ and standard normally distributed random variables $N_i\sim\mathcal{N}(0,1)$. Alternatively, we can choose $S_0$ or $\max_{i=1,\ldots,n}S_i$, etc.\,instead of $\bar{S}$ to obtain similar results. Since for $a,b\in\R$ and a random variable $X\sim\mathcal{N}(\mu,\sigma^2):aX+b\sim\mathcal{N}(a\mu+b, a^2\sigma^2)$, we receive
    \begin{equation*}
        \begin{aligned}
            S^\varepsilon_i=S_i+\varepsilon\bar{S}N_i\sim\mathcal{N}(S_i,\varepsilon^2\bar{S}^2).
        \end{aligned}
    \end{equation*}
    
    The Markowitz model (\ref{problem:Markowitz}) is sensitive to small changes in the price of the stock $S$. Even noised stock prices $S^\varepsilon$ with a $\varepsilon=2\,\%$ deviation cause large changes in the results.
\end{remark}
\begin{figure}[h]
\centering
    \begin{subfigure}{0.5\linewidth}
    \includesvg[width=\linewidth]{assets/plotMeanVarianceMarkowitz05New}
    \caption{$\varepsilon = 0.5\,\%$}
    \end{subfigure}\hfill
    \begin{subfigure}{0.5\linewidth}
    \includesvg[width=\linewidth]{assets/plotMeanVarianceMarkowitz10New}
    \caption{$\varepsilon = 1.0\,\%$}
    \end{subfigure}

    \begin{subfigure}{0.5\linewidth}
    \includesvg[width=\linewidth]{assets/plotMeanVarianceMarkowitz15New}
    \caption{$\varepsilon = 1.5\,\%$}
    \end{subfigure}\hfill
    \begin{subfigure}{0.5\linewidth}
    \includesvg[width=\linewidth]{assets/plotMeanVarianceMarkowitz20New}
    \caption{$\varepsilon = 2.0\,\%$}
    \end{subfigure}
\caption{The mean-variance plot of (\ref{problem:Markowitz})}
\label{fig:plotMeanVarianceMarkowitz}
\end{figure}
\newpage
\subsection{Utility Maximization}
\begin{theorem}
\label{thm:UtilityMaximizationSensitivity}
    The sensitivity of (\ref{eq:utility-maximization}) with respect to $\kappa$ is given by
    \begin{equation*}
        \mathcal{S}(\kappa) = -\frac{1}{\kappa^2}\precision r + \frac{1}{\kappa^2}\frac{\B}{\C}\precision\one.
    \end{equation*}
\end{theorem}
\begin{proof}
    We reformulate the solution (\ref{eq:utility-maximization}) and take the derivative with respect to $\kappa$
    \begin{equation*}
        \begin{aligned}
            x^*(\kappa) 
            &= \frac{1}{\kappa}\precision\left(r+\frac{\kappa-\B}{\C}\one\right)
            = \frac{1}{\kappa}\precision r + \frac{1}{\C}\precision\one - \frac{1}{\kappa}\frac{\B}{\C}\precision \one \\
            \frac{\partial x^*}{\partial\kappa}
            &= -\frac{1}{\kappa^2}\precision r + \frac{1}{\kappa^2}\frac{\B}{\C}\precision\one.
        \end{aligned}
    \end{equation*}
\end{proof}
\begin{remark}
     The sensitivity with respect to $\kappa$ of the model (\ref{problem:UtilityMaximization}) depends on the covariance matrix $\Sigma^{-1}$, the expected return $r$ and the risk aversion $\kappa$ by Theorem \ref{thm:UtilityMaximizationSensitivity}. But the influence of a potentially badly behaved covariance matrix or expected return $r$ shrinks quadratically as $\kappa\to\infty$. Choosing $\kappa\gg0$ sufficiently large, ensure a low-sensitivity model.
\end{remark}
\begin{remark}
    Figure \ref{fig:plotSensitivityElasticityUtilityMaximization} shows that the sensitivity and elasticity of the model (\ref{problem:UtilityMaximization}) vanishes for a sufficiently high risk aversion $\kappa\gg0$, i.e.\,$\lim_{\kappa\to\infty}\mathcal{S}(\kappa)=\lim_{\kappa\to\infty}\mathcal{E}(\kappa)=0$. In addition, we can see $\mathcal{S}(\kappa)=\mathcal{O}(\frac{1}{\kappa^2})$ and $\mathcal{E}(\kappa)=\mathcal{O}(\frac{1}{\kappa})$. This means that the model (\ref{problem:UtilityMaximization}) is not sensitive and completely inelastic for a sufficiently high risk aversion. \medskip
    
    Due to Figure \ref{fig:plotSensitivityElasticityUtilityMaximization} and Theorem \ref{thm:UtilityMaximizationSensitivity}, we can conclude the following statements
    \begin{equation*}
        \begin{aligned}
            \mathcal{S}(\kappa) &= -\frac{1}{\kappa^2}\precision r + \frac{1}{\kappa^2}\frac{\B}{\C}\precision\one = \mathcal{O}(\kappa^{-2}) \\
            \mathcal{E}(\kappa) &= \frac{\kappa}{x^*}\mathcal{S}(\kappa) = \mathcal{O}(\kappa^{-1}).
        \end{aligned}
    \end{equation*}
\end{remark}
\begin{figure}[h]
  \includesvg[width=\linewidth]{assets/plotSensitivityElasticityUtilityMaximizationNew}
  \caption{Sensitivity and Elasticity of the Utility Maximization (\ref{problem:UtilityMaximization}) for varying risk aversion $\kappa$}
  \label{fig:plotSensitivityElasticityUtilityMaximization}
\end{figure}
\begin{remark}
    On the one hand, Figure \ref{fig:plotMeanVarianceUtilityMaximization} shows the efficient frontier according to the Markowitz model. On the other hand, we see the mean-variance plot of portfolios determined by (\ref{eq:utility-maximization}) with noised stock data, cf. Remark \ref{remark:meanVariancePlotMarkowitz}. We have chosen $\kappa=1,\ldots,1\,000$. The Utility Maximization model (\ref{problem:UtilityMaximization}) is also sensitive to the price of the stock $S$, but allocations for sufficiently large $\kappa\gg0$ are less risky than allocations from the Markowitz model. The mean-variance position of the noised-stock allocation tends to locate left at minimal risk. \medskip

    The Utility Maximization model (\ref{problem:UtilityMaximization}) is sensitive to changes in the price of the stock. But for sufficiently high risk aversion, the portfolio remains at low risk.
\end{remark}
\begin{figure}[h]
\centering
    \begin{subfigure}{0.5\linewidth}
    \includesvg[width=\linewidth]{assets/plotMeanVarianceUtilityMaximization05New}
    \caption{$\varepsilon = 0.5\,\%$}
    \end{subfigure}\hfill
    \begin{subfigure}{0.5\linewidth}
    \includesvg[width=\linewidth]{assets/plotMeanVarianceUtilityMaximization10New}
    \caption{$\varepsilon = 1.0\,\%$}
    \end{subfigure}

    \begin{subfigure}{0.5\linewidth}
    \includesvg[width=\linewidth]{assets/plotMeanVarianceUtilityMaximization15New}
    \caption{$\varepsilon = 1.5\,\%$}
    \end{subfigure}\hfill
    \begin{subfigure}{0.5\linewidth}
    \includesvg[width=\linewidth]{assets/plotMeanVarianceUtilityMaximization20New}
    \caption{$\varepsilon = 2.0\,\%$}
    \end{subfigure}
\caption{The mean-variance plot of (\ref{problem:UtilityMaximization})}
\label{fig:plotMeanVarianceUtilityMaximization}
\end{figure}
\newpage\mbox{}\newpage
\subsection{Linear Programming}
\begin{remark}
    Figure \ref{fig:plotSensitivityElasticityLinearProgramming} shows the sensitivity and elasticity of the Linear Programming model (\ref{problem:LinearProgram}) with respect to the minimum return $\mu$. Sensitivity and elasticity vanish until $\mu_1\approx5.5\,\%$. From this point the sensitivity grows to its maximum at $\mu_2\approx10\,\%$ and almost stays at this level for greater minimum returns. However, the elasticity increases to its maximum at $\mu_3\approx33\,\%$ and then shrinks. There are two low-sensitivity or low-elasticity points at $\mu_4\approx22.5\,\%$ and $\mu_5\approx25.5\,\%$. The sensitivity of the Linear Programming model (\ref{problem:LinearProgram}) is greater than the sensitivity of the Markowitz model (\ref{problem:Markowitz}). Consequently, the elasticity is also greater. Furthermore, the sensitivity of the LP did not remain constant, unlike Markowitz. We choose $\alpha=95\,\%$ and $\gamma=50\,\%$.
\end{remark}
\begin{figure}[h]
  \centering
  \includesvg[width=\textwidth]{assets/plotSensitivityElasticityLinearProgrammingMy}
  \caption{Sensitivity and Elasticity of model (\ref{problem:LinearProgram}) for varying minimum return $\mu$}
  \label{fig:plotSensitivityElasticityLinearProgramming}
\end{figure}
\newpage
\begin{remark}
    Figure \ref{fig:plotSensitivityElasticityLinearProgrammingAlpha} shows the sensitivity and elasticity of the Linear Programming model (\ref{problem:LinearProgram}) with respect to level $\alpha$. Sensitivity and elasticity are almost zero and have a jump at $\alpha_1\approx32.5\,\%, \alpha_2\approx37.5\,\%, \alpha_3\approx48\,\%,\alpha_4\approx51\,\%,\alpha_5\approx56\,\%,\alpha_6\approx62.5\,\%,\alpha_7\approx64\,\%,\alpha_8\approx68\,\%,\alpha_9\approx69\,\%,\alpha_{10}\approx70\,\%,\alpha_{11}\approx78\,\%,\alpha_{12}\approx84\,\%$. This means that the model changes only rapidly at these certain points. We set $\gamma=50\,\%$ and $\mu=25\,\%$. The step size is $\Delta\theta=\Delta\alpha=1\,\%$.
\end{remark}
\begin{figure}[h]
  \centering
  \includesvg[width=\textwidth]{assets/plotSensitivityElasticityLinearProgrammingAlpha}
  \caption{Sensitivity and Elasticity of model (\ref{problem:LinearProgram}) for varying level $\alpha$}
  \label{fig:plotSensitivityElasticityLinearProgrammingAlpha}
\end{figure}
\newpage
\begin{remark}
    Figure \ref{fig:plotSensitivityElasticityLinearProgrammingGamma} shows the sensitivity and elasticity of the Linear Programming model (\ref{problem:LinearProgram}) with respect to weight $\gamma$. Sensitivity and elasticity are almost zero and have a jump at $\gamma_1\approx10\,\%$ and $\gamma_2\approx16\,\%$. This means that the model changes only rapidly at these certain points. We choose quantile $\alpha=95\,\%$ and minimum return $\mu=25\,\%$. The step size is $\Delta\theta=\Delta\gamma=1\,\%$.
\end{remark}
\begin{figure}[h]
  \centering
  \includesvg[width=\textwidth]{assets/plotSensitivityElasticityLinearProgrammingGamma}
  \caption{Sensitivity and Elasticity of model (\ref{problem:LinearProgram}) for varying weight $\gamma$}
  \label{fig:plotSensitivityElasticityLinearProgrammingGamma}
\end{figure}
\newpage
\begin{claim}\mbox{}
    \begin{enumerate}[\itshape(i)]
        \item The LP model (\ref{problem:LinearProgram}) is sensitive with respect to the stock price $S$.
        \item The efficient frontier is a piecewise affine function.
    \end{enumerate}
\end{claim}
\begin{remark}
    Figure \ref{fig:plotMeanRiskMeasureLinearProgramming} shows the efficient frontier according to the Linear Programming model (\ref{problem:LinearProgram}) for the risk measure $\rho(x)=\mathcal{R}_{\alpha,\gamma}(-x^\top\xi)=(1-\gamma)\E(-x^\top\xi)+\gamma\AVaR_\alpha(-x^\top\xi)$. The efficient frontier is piecewise affine. In addition, we see the mean-"variance" plot or better mean-$\rho$ plot of portfolios determined by (\ref{problem:LinearProgrammFullMatrix}) with noised stock data, cf.\,Remark \ref{remark:meanVariancePlotMarkowitz}. We set the quantile $\alpha=95\,\%$ and the weight $\gamma=50\,\%$. \medskip
    
    The Linear Programming Model (\ref{problem:LinearProgram}) is also sensitive to the stock price $S$. The mean-$\rho$ position of the noised-stock allocation tends to locate on the left side, or close to the efficient frontier on the right.
\end{remark}
\begin{figure}[h]
\centering
    \begin{subfigure}{0.5\linewidth}
    \includesvg[width=\linewidth]{assets/plotMeanRiskMeasureLinearProgramming05New}
    \caption{$\varepsilon = 0.5\,\%$}
    \end{subfigure}\hfill
    \begin{subfigure}{0.5\linewidth}
    \includesvg[width=\linewidth]{assets/plotMeanRiskMeasureLinearProgramming10New}
    \caption{$\varepsilon = 1.0\,\%$}
    \end{subfigure}

    \begin{subfigure}{0.5\linewidth}
    \includesvg[width=\linewidth]{assets/plotMeanRiskMeasureLinearProgramming15New}
    \caption{$\varepsilon = 1.5\,\%$}
    \end{subfigure}\hfill
    \begin{subfigure}{0.5\linewidth}
    \includesvg[width=\linewidth]{assets/plotMeanRiskMeasureLinearProgramming20New}
    \caption{$\varepsilon = 2.0\,\%$}
    \end{subfigure}
\caption{The mean-risk plot of (\ref{problem:LinearProgram})}
\label{fig:plotMeanRiskMeasureLinearProgramming}
\end{figure}
\newpage

\section{Conclusions}

This elaboration provides a mathematical formulation of risk, numerical approaches for asset allocation models, and an analysis of parameter sensitivity. Therefore, we introduce risk measures for an investor's risk preference and derive numerical solutions for each measure:

\medskip The classical Markowitz model, which minimizes the variance; an Utility Maximization model; and a Linear Programming model, based on minimizing the Average Value at risk. Each of them has strengths and weaknesses that are revealed in the sensitivity analysis. The results show that the efficient Markowitz portfolio is highly sensitive to small changes in the covariance matrix and expected return. In contrast, the Utility Maximization approach produces a stable allocation for sufficiently large risk aversion. The Linear Programming model provides a risk perspective that minimizes extreme losses and behaves less sensitively outside of some tipping points.

\medskip At the same time, the sensitivity analysis highlights important limitations of these three portfolio optimization models: Unfortunately, all of these methods are based on historical data. The results we have received are optimal in the past. Even if we assume that stocks will behave similarly in the future, small changes in the stock prices cause big differences in the outcomes. In the last section, we confirm and quantify this phenomenon with sensitivity measures.

\medskip Nevertheless, an investment strategy shall be measured alongside these three methods and should not be surpassed. Future work may improve these models, e.g. we can hedge against the uncertainties or leverage the expected return.

\bibliographystyle{abbrvnat}
\label{References}
\bibliography{Literatur}

\end{document}